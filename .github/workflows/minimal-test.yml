name: Minimal Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Echo test
        run: echo "Hello from Windows"
      
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: List files
        run: dir
      
      - name: Python version
        run: python --version
