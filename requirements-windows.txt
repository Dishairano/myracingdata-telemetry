# MyRacingData Telemetry Capture - Windows Requirements
# Simplified for reliable PyInstaller builds

# Core dependencies
websocket-client>=1.6.4
requests>=2.31.0

# Desktop UI (modern web UI in a native window; Edge WebView2 backend on Windows)
pywebview>=5.0
# System tray & UI (classic fallback UI)
pystray>=0.19.5
Pillow>=10.1.0

# Packaging
pyinstaller>=6.3.0

# Utilities
python-dotenv>=1.0.0
colorama>=0.4.6

# Note: accapi removed — the ACC broadcasting reader was replaced by the shared
#   memory reader (games/acc_shared_memory.py); accapi>=0.1.0 was also
#   unsatisfiable on PyPI (only 0.0.x exist), which silently failed the whole
#   pip install and shipped an .exe with no dependencies bundled.
# Note: python-socketio removed — unused (the client uses raw websocket-client).
# Note: pywin32 removed - will use ctypes instead for Windows APIs
# Note: mmap-backed-arrays removed - using standard library mmap
